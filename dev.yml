type: go

up:
  - packages:
      - protoc
  - go:
      version: '1.23.1'
      tools:
        - github.com/golang/protobuf/protoc-gen-go@latest

commands:
  generate: protoc --go_out=gen --go_opt=paths=source_relative api/protocol.proto
